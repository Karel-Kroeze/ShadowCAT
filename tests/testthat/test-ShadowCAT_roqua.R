# only for whithin R:
'
library(testthat)
'

if (FALSE) {
make_random_seed_exist <- rnorm(1)

# update person with new answer
person <- answer(person, test, indeces = next_item(person, test))
next_item_person <- ShadowCAT_roqua(person, test)
}